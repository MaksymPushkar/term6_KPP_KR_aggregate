package com.pm.aggregate.repository;

import com.pm.aggregate.entity.ProductStorageTank;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProductStorageTankRepository extends JpaRepository<ProductStorageTank, Integer> {
}
